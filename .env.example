# EmailJS (client-side)
# Create an EmailJS template that uses variables:
#   {{from_name}}, {{reply_to}}, {{subject}}, {{message}}

# Optional (the code already defaults to this service id):
REACT_APP_EMAILJS_SERVICE_ID=service_m0lextk

# Required:
REACT_APP_EMAILJS_TEMPLATE_ID=template_your_template_id
REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key
